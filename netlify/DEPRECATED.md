# Deprecated — Netlify Functions

This folder is **no longer used**. The site is deployed on **Firebase Hosting** with **Cloud Functions** in the `functions/` directory.

API routes are unchanged (`/api/audit`, `/api/contact`, `/api/availability`, `/api/book-slot`, `/api/admin/*`).

See the root [README.md](../README.md) for Firebase deploy instructions.
